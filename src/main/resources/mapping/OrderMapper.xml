<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.OrderMapper">

    <insert id="addOrder" parameterType="com.example.demo.entity.Orders" useGeneratedKeys="true" keyColumn="id" keyProperty="id">
        insert into orders values (null, #{userId}, #{goodsId}, #{money}, #{title}, #{comment}, 1, now(), now())
    </insert>

    <select id="getOrderByUserId" resultType="com.example.demo.entity.Orders">
        select * from orders where userId = #{userId} and flag = 2
    </select>

    <update id="updateOrderById" parameterType="integer">
        update orders set flag = #{flag}, updateTime = now() where id = #{id}
    </update>
</mapper>
